name: "CodeQL"

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
      - develop
  schedule:
    - cron: '36 13 * * 1'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest

    permissions:
      actions: read # for github/codeql-action/init to get workflow details
      contents: read # for actions/checkout to fetch code
      security-events: write # for github/codeql-action/analyze to upload SARIF results

    strategy:
      fail-fast: false
      matrix:
        language: [ 'go' ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # pin@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@e96cea3a2effd357d270bca8686d8d8b04ee0773 # pin@v2
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@e96cea3a2effd357d270bca8686d8d8b04ee0773 # pin@v2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@e96cea3a2effd357d270bca8686d8d8b04ee0773 # pin@v2
